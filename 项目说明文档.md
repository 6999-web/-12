# 智能实验室 — 项目说明文档

> 本文档记录 `新建文件夹 (2)` 中所有代码的功能、架构与技术栈，便于后续开发与维护。

---

## 目录

1. [项目总览](#项目总览)
2. [项目一：310 正常 3D 交互场景](#项目一310-正常-3d-交互场景)
   - [技术栈](#技术栈)
   - [项目结构](#项目结构)
   - [3D 场景架构](#3d-场景架构)
   - [交互系统](#交互系统)
   - [导航系统](#导航系统)
   - [模型编辑器](#模型编辑器)
   - [3D 模型资源](#3d-模型资源)
   - [数据流与状态管理](#数据流与状态管理)
   - [运行方式](#运行方式)
3. [项目二：Vue 3 前端界面](#项目二vue-3-前端界面)
   - [项目内容](#项目内容)
   - [设计文档](#设计文档)
4. [后续开发建议](#后续开发建议)

---

## 项目总览

本文件夹包含智能实验室（Smart Lab）的两个前端项目：

| 项目 | 技术框架 | 用途 |
|------|---------|------|
| `310正常3d交互场景` | React + Three.js (R3F) | 310 实验室数字孪生 3D 交互场景 |
| `新建文件夹` | Vue 3 + Vite | 智能实验室 Web 前端主界面（含设计文档） |

---

## 项目一：310 正常 3D 交互场景

**路径**: `310正常3d交互场景/310正常3d交互场景/`

这是一个基于 **React Three Fiber** 的 310 智能实验室数字孪生 MVP 场景，采用"前端建筑层 + GLB 家具层 + Collider 交互层"的架构。

### 技术栈

| 类别 | 技术 |
|------|------|
| 前端框架 | React 18.3 |
| 语言 | TypeScript 5.7 |
| 构建工具 | Vite 5.4 |
| 3D 引擎 | Three.js 0.164 |
| React 3D 绑定 | @react-three/fiber 8.17 |
| 3D 工具库 | @react-three/drei 9.122 |
| CSS 框架 | Tailwind CSS 3.4 |
| 图标库 | Lucide React 0.468 |

### 项目结构

```
310正常3d交互场景/310正常3d交互场景/
├── index.html                          # 入口 HTML
├── package.json                        # 依赖配置
├── package-lock.json                   # 依赖锁定
├── vite.config.ts                      # Vite 配置
├── tsconfig.json                       # TypeScript 配置
├── tailwind.config.js                  # Tailwind CSS 配置
├── postcss.config.js                   # PostCSS 配置
├── public/
│   ├── model/
│   │   ├── workstation_area.glb        # 80 工位整体模型
│   │   ├── meeting_area.glb            # 会议桌 + 会议大屏模型
│   │   └── hardware_area.glb           # 硬件工位 + 3 台 3D 打印机模型
│   └── textures/
│       ├── display_wall_01.jpg         # 成果展示墙贴图1
│       ├── display_wall_02.jpg         # 成果展示墙贴图2
│       └── people_wall.png.jpg         # 人员展示墙贴图
├── src/
│   ├── main.tsx                        # React 入口
│   ├── styles.css                      # 全局样式 + Tailwind
│   └── components/
│       ├── scene/                      # 3D 场景组件
│       │   ├── constants.ts            # 场景常量（房间尺寸、材质、颜色）
│       │   ├── types.ts                # 类型定义
│       │   ├── LabScene.tsx            # 场景主入口（Canvas + 所有子系统）
│       │   ├── FloorSystem.tsx         # 地板系统（瓷砖网格 + 区域标记 + HUD 路径）
│       │   ├── WallSystem.tsx          # 墙体系统（墙体 + 展示墙 + 发光边条）
│       │   ├── LightSystem.tsx         # 灯光系统（环境光 + 方向光 + 点光源 + 线形灯）
│       │   ├── ModelAssets.tsx         # GLB 模型加载与管理
│       │   ├── PlaceholderAssets.tsx   # 占位模型
│       │   ├── FlyKeyboardControls.tsx # WASD 键盘漫游控制
│       │   ├── SelectionSystem.tsx     # 点击选择系统（Raycaster）
│       │   ├── SelectionHighlight.tsx  # 选中高亮（外框 + 扫描线 + 信息卡）
│       │   ├── SelectionBeam.tsx       # 选中光束扫描特效
│       │   ├── SelectionPulse.tsx      # 选中脉冲扩散特效
│       │   ├── SelectionScanRing.tsx   # 选中扫描环特效
│       │   ├── ZoneMarker.tsx          # 区域标记组件
│       │   └── modelEditorStore.ts     # 模型编辑器状态管理（localStorage 持久化）
│       └── ui/                         # UI 组件
│           ├── ModelEditorPanel.tsx    # 右下角模型编辑面板
│           └── ObjectInfoCard.tsx      # 点击对象信息卡片
└── 3D交付说明.md                       # 详细的交付说明文档
```

### 3D 场景架构

场景采用分层架构，所有 3D 元素挂载在 `Lab_310_DigitalTwin_Base` 根节点下：

#### 1. 地板系统 (`FloorSystem.tsx`)
- **瓷砖底座**: 深色底座网格 (`Floor_Tile_Base`)
- **600mm 瓷砖网格线**: 按标准瓷砖尺寸绘制网格线
- **区域标记**: 三个功能区域的半透明标记框
  - 工位区 (Zone_Workstations): 13.7m × 6.8m
  - 会议区 + 大屏 (Zone_Meeting): 4.65m × 5.9m
  - 硬件工位区 (Zone_Hardware): 6.2m × 1.55m
- **HUD 导航路径**: 发光路径线，模拟实验室地面导航指引

#### 2. 墙体系统 (`WallSystem.tsx`)
- **基础墙体**: 左右两侧 + 底侧墙体，使用半透明材质
- **顶部墙体**: 包含两个门洞（Door 01, Door 02）的顶部墙体
- **结构横梁**: 四条顶部结构梁，模拟建筑框架
- **发光边条**: 墙角、门框位置的蓝色发光装饰线
- **墙体面板线**: 横向 + 纵向的装饰分割线
- **展示墙**:
  - `Zone_DisplayWall_01`: 成果展示墙1（8.4m，顶部主展示墙）
  - `Zone_DisplayWall_02`: 成果展示墙2（4.3m，工位右侧）
  - `Zone_PeopleWall`: 人员展示墙（3.8m，左侧）

#### 3. 灯光系统 (`LightSystem.tsx`)
| 灯光类型 | 名称 | 用途 |
|---------|------|------|
| AmbientLight | Ambient_CoolWhite | 基础冷白环境光 (强度 1.25) |
| HemisphereLight | Hemisphere_Lab_Fill | 半球补光（天空白 + 地面蓝灰） |
| DirectionalLight | Main_Soft_Key | 主方向光（带阴影贴图 2048×2048） |
| PointLight × 8 | Edge_Blue, DisplayWall, Zone | 各区域蓝色氛围点光源 |
| 线形灯 × 4 | Ceiling_Linear_Light | 天花线形灯（工位区 × 2、会议区、硬件区） |

#### 4. 场景环境
- **天空**: `Sky` 组件（距离 450000，模拟室外天光）
- **雾效**: 线性雾（颜色 #DDEBFA，近 18m，远 42m）
- **接触阴影**: `ContactShadows`（软阴影，模糊 2.6）
- **色调映射**: ACES Filmic（曝光 1.22）
- **色彩空间**: SRGBColorSpace
- **辅助工具**: 右上角 XYZ 方向轴 (GizmoViewport)

### 交互系统

#### 点击选择 (`SelectionSystem.tsx`)
- **机制**: 仅对 collider 节点进行 raycast，不直接检测复杂 GLB mesh
- **交互流程**:
  1. 用户点击 Canvas
  2. 判断是否为拖拽（移动距离 > 5px 视为拖拽，不触发选择）
  3. 收集场景中所有可见的 `_collider` 节点
  4. Raycaster 射线检测
  5. 命中后读取 collider 的 `userData` 元数据
  6. 传递给高亮和信息卡显示
- **退出选择**: 点击空白处 或 按 Esc 键

#### 选中高亮 (`SelectionHighlight.tsx`)
- **蓝色呼吸外框**: 使用 `EdgesGeometry` 绘制发光边框，opacity 以 2.4Hz 正弦波呼吸
- **扫描光束** (`SelectionBeam.tsx`): 垂直方向上下循环扫描的水平光条
- **扫描环** (`SelectionScanRing.tsx`): 向外扩散的环形特效
- **脉冲** (`SelectionPulse.tsx`): 点击瞬间的脉冲扩散动画
- **信息卡片** (`ObjectInfoCard.tsx`): 使用 `Html` 组件在 3D 空间中渲染 HTML 卡片
  - 显示：名称、类型、状态、描述
  - 带渐隐退出动画（200ms delay）
  - 半透明玻璃质感（backdrop-blur + 阴影）

#### Collider 系统 (`ModelAssets.tsx`)
- **工位区**: 使用 GLB 模型内置的 `seat_rXX_cXX_collider` 节点（80 个工位）
- **会议区/硬件区**: 前端根据 root 节点自动生成透明 bbox collider
- **Collider 材质**: 完全透明（opacity: 0），不写入深度缓冲
- **元数据绑定**: 每个 collider 的 `userData` 包含：
  - `object_id`, `display_name`, `type`, `status`, `description`
- **工位 collider 元数据规则**: `seat_r{行}_c{列}_collider` 格式自动解析

### 导航系统

#### 键盘漫游 (`FlyKeyboardControls.tsx`)
| 按键 | 功能 |
|------|------|
| W / S | 前 / 后移动 |
| A / D | 左 / 右平移 |
| E / Q | 升 / 降 |
| Shift | 加速（速度 ×2，5→10） |

- **移动范围限制**: 在房间范围内（含 0.65m 边距）
- **高度限制**: 1.5m ~ 18m
- **帧率无关**: 使用 `delta` 确保不同帧率下速度一致（上限 50ms）
- **OrbitControls 联动**: 键盘移动时同步更新 OrbitControls 的 target

#### 鼠标轨道控制
- 使用 Drei 的 `OrbitControls`
- 阻尼因子 0.08
- 极角上限 87.8°（防止翻到底部）
- 距离范围 5m ~ 26m

### 模型编辑器

#### 编辑面板 (`ModelEditorPanel.tsx`)
- **位置**: 右下角固定定位
- **可编辑对象**:
  - 工位区 (workstation_area.glb)
  - 会议区 (meeting_area.glb)
  - 硬件区 (hardware_area.glb)
- **可调参数**:
  | 参数 | 步长 | 范围 |
  |------|------|------|
  | X 轴位置 | 0.1 | 无限制 |
  | Y 轴位置 | 0.1 | 无限制 |
  | Z 轴位置 | 0.1 | 无限制 |
  | Y 轴旋转 | 5° | 无限制 |
  | 整体大小 | 0.05 | ≥ 0.1 |
- **操作**: 每个轴有 +/- 按钮和直接数值输入框
- **重置**: 支持"重置当前"和"全部重置"
- **持久化**: 编辑状态保存在 `localStorage`（key: `lab310:model-editor:v1`）

#### 状态管理 (`modelEditorStore.ts`)
- 自定义外部 Store（基于 `useSyncExternalStore`）
- 默认变换值硬编码在 `editableModelConfigs`
- 从 localStorage 加载/保存，异常时回退到默认值
- 数值校验：`Number.isFinite` 检查，scale 最小 0.1

### 3D 模型资源

| 文件 | 大小 | 描述 |
|------|------|------|
| `workstation_area.glb` | - | 80 个工位整体模型，包含 collider 节点 |
| `meeting_area.glb` | - | 会议桌 + 会议大屏 |
| `hardware_area.glb` | - | 硬件工作台 + 3 台 3D 打印机 |

**重要说明**:
- 工位区作为整体移动/缩放，不支持单独移动某个工位
- Collider 点击机制：只检测 collider 节点，不直接 raycast 复杂 mesh
- 模型中的旧节点（`zz_old_*`、`_disabled`）自动隐藏

### 数据流与状态管理

```
用户点击 → SelectionSystem (raycaster)
         → 命中 collider
         → 读取 userData (object_id, display_name, type, status, description)
         → SelectionHighlight (3D 高亮特效 + HTML 信息卡)
         
编辑器操作 → ModelEditorPanel
          → updateModelTransform() 
          → modelEditorStore (内存 + localStorage)
          → ModelAssets 重渲染（应用新位置/旋转/缩放）
```

### 运行方式

```bash
# 安装依赖
npm install

# 启动开发服务器
npm run dev

# 构建生产版本
npm run build

# 预览构建结果
npm run preview
```

---

## 项目二：Vue 3 前端界面

**路径**: `新建文件夹/新建文件夹/`

这是一个基于 **Vue 3 + Vite** 的智能实验室 Web 前端主界面项目。

### 项目内容

```
新建文件夹/新建文件夹/
├── README.md                    # Vue 3 + Vite 项目说明
├── .gitignore                   # Git 忽略规则
├── .vscode/
│   └── extensions.json          # VS Code 推荐扩展
├── .codex-run/
│   ├── frontend.err.log         # 前端运行错误日志
│   └── frontend.out.log         # 前端运行输出日志
├── dist/                        # 构建产物
│   ├── index.html               # 构建后的入口页面
│   ├── favicon.svg              # 网站图标
│   ├── icons.svg                # SVG 图标集
│   └── assets/
│       ├── index-CP20eBoQ.js    # 打包后的 JS
│       ├── index-C-5D2eDF.css   # 打包后的 CSS
│       ├── mascot.png           # 吉祥物/助手图标
│       └── image1.png ~ image8.png  # 页面展示图片
├── extracted/                   # 解压的 Word 文档1
│   ├── text_utf8.txt            # 文档纯文本
│   └── word/
│       ├── document.xml         # 文档 XML
│       └── media/
│           └── image1.png ~ image8.png  # 文档内嵌图片
└── extracted_narrative/         # 解压的 Word 文档2
    ├── text_utf8.txt            # 文档纯文本
    ├── docProps/
    │   ├── app.xml              # 文档属性
    │   └── core.xml             # 核心属性
    └── word/
        ├── document.xml         # 文档 XML
        ├── header1.xml          # 页眉
        ├── footer1.xml          # 页脚
        ├── embeddings/
        │   └── Microsoft_Excel_Worksheet1.xlsx ~ 14.xlsx  # 内嵌 Excel 表格 (14个)
        └── media/
            └── image1.png ~ image25.png  # 文档内嵌图片 (25张)
```

### 设计文档

项目包含两份从 Word 文档解压出的设计说明：

#### 文档 1：智能实验室——前端优化 v4 (`extracted/text_utf8.txt`)
- **首界面设计**: 实验室首页的整体布局优化
- **弹窗设计**:
  1. 点击右上角在线人员 → 弹出人员详情
  2. 点击右上角会议室 → 弹出会议室详情
  3. 点击右侧设备状态 → 弹出设备详情
  4. 点击右下角实验室运行事件流 → 弹出事件列表
- **空间状态**: 空间设备 → 设备状态列表 / 空间选择 → 弹窗详情

#### 文档 2：智能实验室—WEB 前端主界面—讲述 (`extracted_narrative/text_utf8.txt`)
- **页面整体结构**: "中间数字孪生空间 + 左右数据面板"
- **导航栏**: 首页总览 → 成员大厅 → 成果展示 → 项目展厅 → 活动风采 → 空间设备 → AI 助手
- **布局字段**:
  - **顶部状态栏**: 系统环境、运行状态（科技感展示，数据实时刷新）
  - **左侧运行概览**: 成员数量、项目数量、论文成果、获奖成果、知识产权（卡片式展示）
  - **左侧本月动态**: 最近实验室动态（时间轴展示）
  - **中间实验室空间**: 310 实验室 3D 空间效果 + 设备点位悬浮标签
  - **右侧实验空间/设备状态**: 实验室是否使用中、会议室空闲状态、设备在线状态
  - **底部小喵助手**: AI 助手入口（智能问答、成果检索、项目查询、设备状态查询）
- **设计理念**: 首页既是实验室的数字大屏，也是系统功能入口

---

## 后续开发建议

根据 `3D交付说明.md` 中的规划，后续工作优先级如下：

### 优先级 1：工位数据绑定
建立 `seat_rXX_cXX → 员工 ID → 员工姓名 → 在线状态 → 项目 → 设备状态` 的完整数据映射。
当前点击工位只能显示占位信息，需要增加业务数据层。

### 优先级 2：统一命名规范
建议持续使用以下命名约定：
- `seat_r{行}_c{列}_root / _desk / _chair / _device / _collider`
- `meeting_table_root / meeting_screen_root`
- `hardware_workbench_root / printer_3d_0{编号}_root`

### 优先级 3：模型可见性控制
根据业务状态动态控制：
- 员工在线/离线
- 工位有人/无人
- 设备在线/离线/故障
- 3D 打印机运行/空闲
- 会议室使用中/空闲

### 优先级 4：性能优化
- 避免加载未使用的大 GLB
- 不将 node_modules / dist 上传 Git
- 复杂模型继续使用 collider-only 点击
- 大文件使用 Git LFS 或 CDN

---

> 📅 文档生成日期：2026-06-09  
> 🏷️ 分支：develop/6.9  
> 🔗 目标仓库：https://gitee.com/gxpc/smart-lab.git
